import '@webcomponents/custom-elements';
import CodigaElement from "./CodigaElement";

/**
 * Base wrapper element on top of the code container that contains the StatusButton
 * and has the same size as the container itself
 */
export default class CodigaExtension extends CodigaElement {
  constructor() {
    super();
  }
}