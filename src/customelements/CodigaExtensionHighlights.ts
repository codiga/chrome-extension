import '@webcomponents/custom-elements';
import CodigaElement from "./CodigaElement";

/**
 * Base wrapper element on top of the code container that contains the highlights and tooltips on code violations
 */
export default class CodigaExtensionHighLights extends CodigaElement {
  constructor() {
    super();
  }
}

